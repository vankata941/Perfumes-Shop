spring:
  application:
    name: OnlineShop
  jpa:
    properties:
      hibernate:
        format_sql: 'TRUE'
    hibernate:
      ddl-auto: update
    open-in-view: 'false'
  sql:
    init:
      mode: never

forex:
  api:
    key: test
    url: "https://example.com"
    base: "USD"